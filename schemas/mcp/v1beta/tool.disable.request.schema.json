{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "the-one.mcp.v1beta.tool.disable.request",
  "title": "tool.disable request",
  "type": "object",
  "required": ["tool_id", "project_root", "project_id"],
  "properties": {
    "tool_id": { "type": "string", "description": "ID of the tool to disable" },
    "project_root": { "type": "string", "description": "Project root path" },
    "project_id": { "type": "string", "description": "Project identifier" }
  },
  "additionalProperties": false
}
